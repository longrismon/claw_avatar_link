services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    environment:
      - VITE_WS_URL=ws://localhost:8000/ws
    depends_on:
      - backend
